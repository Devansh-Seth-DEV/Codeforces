#!/bin/bash

CONTEST_TYPE="$1"
CONTEST_NO="$2"
CONTEST_DIR="./contests"
CONTEST_TYPE_DIR="${CONTEST_DIR}/div${CONTEST_TYPE}"
NEW_CONTEST_DIR="${CONTEST_TYPE_DIR}/${CONTEST_NO}"

mkdir -p $NEW_CONTEST_DIR
